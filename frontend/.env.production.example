# Frontend production env — copy to .env.production. Real file is gitignored.
VITE_API_URL=/api/v1
# Stripe publishable key (safe to expose). Leave empty to keep the mock flow.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_or_pk_live_...
